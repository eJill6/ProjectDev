@using JxBackendService.Model.Paging
@using JxBackendService.Model.ViewModel.Game
@model PagedResultModel<LiveGameManageModel>
<div>
    <table>
        <tbody class="jqBody">
            @for (int i = 0; i < Model.ResultList.Count; i++)
            {
                var item = Model.ResultList[i];
                <tr>
                    <td>@(i+1)</td>
                    <td>@item.LotteryType</td>
                    <td>@item.LiveGameManageDataTypeName</td>
                    <td class="image_in_grid hot_game_image">
                        <img aes-src="@item.AesFullImageUrl" />
                    </td>
                    <td>@item.Url</td>
                    <td>@item.ApiUrl</td>                    
                    <td>@item.FrameRatioText</td>
                    <td>@item.StyleText</td>
                    <td>@item.IsCountdownText</td>
                    <td>@item.DurationText</td>
                    <td>@item.IsH5Text</td>
                    <td>@item.IsFollowText</td>
                    <td>@item.Sort</td>
                    <td>@item.IsActiveText</td>
                    <td>
                        <div class="flex_content">
                            @await Html.PartialAsync("Partial/_OperationButton", item)
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div class="jqPagination">
        @await Html.PartialAsync("Partial/_PaginationByPagerInfo", Model)
    </div>
</div>